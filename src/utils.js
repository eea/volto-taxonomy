const fixState = (state) => {
  // We need to handle the state sent to the backend here
  // If we allow arbitrary input of new items in the taxonomy editor

  console.log(state);
  return state;
};

export { fixState };
